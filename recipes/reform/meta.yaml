{% set name = "reform" %}
{% set version = "1.1.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://github.com/gencorefacility/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz"
  sha256: 4e0e944250ec98d5165a844cdff1074194f0cf8061ef2e97ffb2446072a400d4

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python
    - pgzip
    - biopython

test:
  imports:
    - reform

about:
  home: "https://github.com/gencorefacility/reform"
  license: "ISC"
  license_family: OTHER
  license_file: LICENSE
  summary: "A python-based command line tool that allows for fast, easy and robust editing of reference genome sequence and annotation files"
  doc_url: "https://gencore.bio.nyu.edu/reform"
  dev_url: "https://github.com/gencorefacility/reform"

extra:
  recipe-maintainers:
    - mohammedkhalfan
    - YuWei-CH
    - genericdata